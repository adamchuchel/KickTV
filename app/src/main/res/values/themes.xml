<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.KickTV.Home" parent="Theme.Leanback.Browse">
        <item name="defaultBrandColor">@color/surface</item>
        <item name="defaultSearchColor">@color/kick_green</item>
        <item name="defaultSearchIconColor">@color/background</item>
        <item name="android:windowBackground">@color/background</item>
        <item name="android:colorBackground">@color/background</item>
    </style>

    <style name="SettingChip">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">52dp</item>
        <item name="android:layout_marginEnd">4dp</item>
        <item name="android:background">@drawable/btn_player_selector</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:gravity">center</item>
        <item name="android:minWidth">80dp</item>
        <item name="android:orientation">vertical</item>
        <item name="android:paddingEnd">12dp</item>
        <item name="android:paddingStart">12dp</item>
    </style>

    <style name="Theme.KickTV" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@color/background</item>
        <item name="colorPrimary">@color/kick_green</item>
        <item name="colorAccent">@color/kick_green</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>
</resources>
